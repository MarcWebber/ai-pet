# Security Policy

## Supported versions

当前只维护最新的 macOS `0.9.x` 测试版本。更早预览包不会继续接收安全修复。

## Reporting a vulnerability

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。
修复发布前，请不要在公开 Issue 中披露可直接利用的细节、凭据或个人截图。

报告中建议包含：

- JellyPet、macOS 和 Codex 版本；
- 最小复现步骤与实际、预期行为；
- 是否涉及截图、Beta 接管、认证环境、临时文件、配置或本地历史；
- 已脱敏的日志或截图。

## Product security boundary

JellyPet 同时提供截图问答与屏幕接管。屏幕接管默认选中，但仍是明确标记的 Beta 功能：

- 主聊天窗口始终显示有明显颜色状态的接管开关，关闭后回到只观察的截图问答；
- 只有在可信环境中才应使用 Beta 接管；
- 工作中可随时关闭接管开关或通过全局唤醒快捷键停止；
- 本地 Codex 子进程继承当前用户的进程环境和认证状态；
- JellyPet 不把 Codex 凭据写入配置或发布包；
- 系统屏幕录制、辅助功能、安全桌面和文件权限仍是最终边界；
- 网页和文档内容是不可信输入，可能尝试诱导 Agent 偏离用户目标。

用户交付的任务目标会被视为本次执行授权；JellyPet 不会按操作类别追加二次审批、只读、
确认或暂停。组合操作只顺序调用观察与原子动作，普通点击仍可执行用户任务所需的操作，
而且工具不会保存跨调用或跨重启的幂等记录。
因此，用户应在开启接管前确认自己确实希望执行完整任务，并在完成后检查实际结果。

## 未发布接管审计

当前实现已删除 Workflow/Ledger、其他 Agent Runtime 和终端兜底；截图问答与屏幕接管
都只进入 Codex app-server。工具完成不等于业务结果完成，仍必须以动作后的最新观察为准。

截图先保存在内存；附加给 Codex app-server 时创建本轮临时文件，轮次结束立即删除。
最近问题与回答以文字形式保存在本机。
删除应用不会自动删除偏好或 `~/Library/Application Support/JellyPet` 中的配置与自定义
精灵图；用户需要自行删除这些本地数据。
