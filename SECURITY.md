# Security Policy

## Supported versions

当前只维护最新的 macOS `0.9.x` 测试版本。更早预览包不会继续接收安全修复。

## Reporting a vulnerability

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。
修复发布前，请不要在公开 Issue 中披露可直接利用的细节、凭据或个人截图。

报告中建议包含：

- JellyPet、macOS 和 Agent Runtime 版本；
- 最小复现步骤与实际、预期行为；
- 是否涉及截图、Beta 接管、认证环境、临时文件、配置或本地历史；
- 已脱敏的日志或截图。

## Product security boundary

JellyPet 同时提供截图问答与屏幕接管。屏幕接管默认选中，但仍是明确标记的 Beta 功能：

- 主聊天窗口始终显示带 `BETA` 标记的接管开关，关闭后回到只观察的截图问答；
- 只有在可信环境中才应使用 Beta 接管；
- 工作中可随时关闭接管开关或通过全局唤醒快捷键停止；
- 本地 Agent Runtime 子进程继承当前用户的进程环境和认证状态；
- JellyPet 不把 Runtime 凭据写入配置或发布包；
- 系统屏幕录制、辅助功能、安全桌面和文件权限仍是最终边界；
- 网页和文档内容是不可信输入，可能尝试诱导 Agent 偏离用户目标。

当前开发版要求发送、提交、购买和删除等外部副作用使用带后置条件的单次激活动作组，
不得使用普通点击。这是 Agent 与工具接口的行为约束，不是事务保证，也不能证明动作在
应用重启后只发生一次。

用户交付的任务目标会被视为本次执行授权；JellyPet 不会在发送、提交、购买或删除前追加
二次审批、确认或暂停。因此，用户应在开启接管前确认自己确实希望执行完整任务，并在
完成后检查实际结果。若操作后的状态无法确认，Agent 应停止并报告待对账，而不是再次
触发同一外部副作用。

## 未发布接管审计

从 `main@52fc081` 开始演进的未发布接管重构已经删除未接入的 Workflow/Ledger 和终端 Runtime
第二套接管循环。屏幕接管只进入 Codex/TraeX app-server 路径，Claude Code/OpenCode
只保留截图问答。

仍有两项边界没有闭合：

- 当前会话只能避免在同一未变化界面上立即重复激活，不能宣称跨 App 重启幂等。
- TraeX 接管和 OpenCode 截图问答的内置工具限制尚未完成逐项实测。无法证明工具面符合
  产品边界前，不应把提示词约束当成操作系统级隔离。

在以上问题解决并完成真实桌面验收前，不应把未发布开发版作为付款、删除或其他不可逆
操作的可靠执行环境。详细状态见 [项目交接文档](./docs/HANDOFF.md)。

截图存放在系统临时目录并在任务结束后删除。最近问题与回答以文字形式保存在本机。
删除应用不会自动删除偏好或 `~/Library/Application Support/JellyPet` 中的配置与自定义
精灵图；用户需要自行删除这些本地数据。
