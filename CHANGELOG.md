# Changelog

## [Unreleased]

### Added

- 新增稳定语义 locator，可按应用、窗口、页面网址、角色、名称、值、祖先和序号在最新
  观察中重新定位元素。
- 新增单次激活并验证动作，用于约束发送、提交、购买和删除等外部副作用。
- 新增语义与视觉联合的进展监管，在动作预算耗尽、连续无变化或连续无法观察时停止接管。
- 新增主聊天气泡布局验证，并把行为检查按定位、层级、单次激活和进展监管拆分。

### Changed

- 主聊天窗口把两个模式 Tab 合并为一个持久化的 `BETA` 接管开关，设置页不再保留重复
  开关。
- 浏览器和原生 Accessibility 快照补充父子层级与结构角色。
- 接管 Skill 更名为 `jellypet-takeover`，改为中文描述通用界面接管契约。
- 屏幕接管只保留 Codex/TraeX app-server 路径；Claude Code/OpenCode 继续支持截图问答，
  选择接管时返回明确错误。

### Removed

- 删除只面向考试场景的 `human-exam-taking` 打包 Skill。
- 删除没有生产调用者的 Workflow/Ledger、对应实现复述测试，以及终端 Runtime 的第二套
  JSON 接管循环和 40 轮预算。
- 删除损坏截图的原始字节指纹兜底、一次性主气泡渲染入口、旧 Codex 专用错误枚举和无
  调用者的设置组件。
- 删除重复包装输入、滚动与观察循环的动作组、独立 URL Policy、原生元素 Registry 和
  Coordinator 内重复保存的模型对话；对应职责回到 `ScreenAction`、Accessibility
  Provider 与 Agent Runtime 会话。

> 当前未发布代码仍有 TraeX/OpenCode 工具边界和真实 E2E 待办，不应直接发布。详见
> [项目交接文档](./docs/HANDOFF.md)。

## [0.9.2] - 2026-08-19

### Changed

- 屏幕接管改为默认工作模式，旧的 schema 1 配置会迁移到 schema 2 并采用新默认值。
- 聊天窗口始终保留“截图问答 / 屏幕接管 · Beta”模式 Tab，不再因切回截图问答而隐藏接管入口。
- 设置页重做为四组粉彩卡片，统一圆角字体、彩色图标、Beta 徽标、粉彩输入面和精确居中的胶囊按钮。
- 版本升级至 0.9.2（build 15）。

### Fixed

- 修复关闭默认接管后无法在聊天窗口直接切回接管模式的问题。

## [0.9.1] - 2026-08-18

### Added

- 新增 `~/Library/Application Support/JellyPet/config.json`，统一保存对话轮数、
  Runtime、模型、思考强度、自定义指令、宠物外形和 Beta 开关。
- 对话上下文与本地回答历史支持配置为 1–50 轮。
- 支持导入一张 8×8 PNG，为 8 种状态分别提供 8 帧动画。
- 设置页可直接定位配置文件、导入或恢复宠物外形。

### Changed

- 项目回到 macOS 单平台开发与发布，删除非 macOS 源码和构建链路。
- 模型 ID 改为独立完整宽度输入框；自定义指令改为可换行、可滚动编辑器。
- 屏幕接管明确标记为 Beta 并默认关闭；普通截图问答不再显示接管模式入口。
- Claude Code 只识别正式 `claude` 命令，不再保留 `cc` 别名。
- Runtime 只从 `PATH` 和常用 CLI 目录探测，删除旧的 App 内置路径兼容层。
- 版本升级至 0.9.1（build 14）。

### Fixed

- 修复 macOS 设置页模型配置和自定义指令输入区域过长、内容被截断的问题。
- 自定义精灵图严格按单元格绘制，避免跨状态串帧；内置完成动画继续保留顶部跳跃空间。

### Removed

- 删除旧平台测试包、构建脚本、过期发布说明和独立接管插件兼容层。
